# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/dayflow_db"

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration (comma-separated origins, or leave for defaults: localhost:3000, localhost:5173)
CORS_ORIGIN="http://localhost:3000,http://localhost:5173"

